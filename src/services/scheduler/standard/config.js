/**
 * Configuración y constantes del scheduler estándar
 */

const CONFIG = {
  MAX_RETRIES: 3,
  RETRY_DELAY_MS: 60000,
  BATCH_LIMIT: 1000,
  CONCURRENT_BUSINESSES: 5,
  BUSINESS_TIMEOUT_MS: 20 * 60 * 1000,
  DELAY_BETWEEN_MESSAGES_MIN: 10000,
  DELAY_BETWEEN_MESSAGES_MAX: 20000,
  DELAY_BETWEEN_MESSAGES_HUMAN_MIN: 25000,
  DELAY_BETWEEN_MESSAGES_HUMAN_MAX: 45000,
  SYNC_WAIT_MS: 2000,
  DELAY_BETWEEN_BATCHES: 5000,
  RESPONSE_WAIT_MS: 0,
  MAX_MESSAGES_PER_MINUTE: 3,
  HUMAN_PAUSE_INTERVAL: 15,
  HUMAN_PAUSE_MIN_MS: 2 * 60 * 1000,
  HUMAN_PAUSE_MAX_MS: 5 * 60 * 1000,
};

module.exports = CONFIG;
