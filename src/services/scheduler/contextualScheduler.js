/**
 * Scheduler Contextual - Envío basado en flujo natural del negocio
 * Modularizado para mejor mantenimiento.
 */

const contextual = require('./contextual/index');

module.exports = {
  ...contextual
};
